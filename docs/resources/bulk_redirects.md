---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "vercel_bulk_redirects Resource - terraform-provider-vercel"
subcategory: ""
description: |-
  Provides a Bulk Redirects resource.
  This resource manages the live project-level bulk redirects for a Vercel project.
  Each apply stages the configured redirect set and promotes that version to production.
---

# vercel_bulk_redirects (Resource)

Provides a Bulk Redirects resource.

This resource manages the live project-level bulk redirects for a Vercel project.
Each apply stages the configured redirect set and promotes that version to production.

## Example Usage

```terraform
resource "vercel_project" "example" {
  name = "example-project"
}

resource "vercel_bulk_redirects" "example" {
  project_id = vercel_project.example.id

  redirects = [
    {
      source                = "/old-path"
      destination           = "/new-path"
      status_code           = 307
      case_sensitive        = false
      query                 = false
    },
    {
      source                = "/blog"
      destination           = "https://example.com/blog"
      status_code           = 308
      case_sensitive        = true
      query                 = true
    },
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `project_id` (String) The ID of the Vercel project to manage redirects for.
- `redirects` (Attributes Set) The full set of live bulk redirects for the project. (see [below for nested schema](#nestedatt--redirects))

### Optional

- `team_id` (String) The ID of the Vercel team.

### Read-Only

- `id` (String) The unique identifier for this resource.
- `version_id` (String) The ID of the live bulk redirects version managed by this resource.

<a id="nestedatt--redirects"></a>
### Nested Schema for `redirects`

Required:

- `destination` (String) The destination pathname or URL to redirect to.
- `source` (String) The source pathname to match.
- `status_code` (Number) The HTTP status code for the redirect.

Optional:

- `case_sensitive` (Boolean) Whether the source match is case-sensitive.
- `query` (Boolean) Whether query parameters are considered when matching the redirect.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# If importing with a team configured on the provider, simply use the project ID.
# - project_id can be found in the project `settings` tab in the Vercel UI.
terraform import vercel_bulk_redirects.example prj_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Alternatively, you can import via the team_id and project_id.
# - team_id can be found in the team `settings` tab in the Vercel UI.
# - project_id can be found in the project `settings` tab in the Vercel UI.
terraform import vercel_bulk_redirects.example team_xxxxxxxxxxxxxxxxxxxxxxxx/prj_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
```
