---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "vercel_audit_log_drain Resource - terraform-provider-vercel"
subcategory: ""
description: |-
  Provides an Audit Log Drain resource.
  Audit Log Drains forward team activity events to an HTTP endpoint or Amazon S3. They apply to the whole team and do not support project selection, filtering, or sampling.
  ~> Audit Log Drains are only available to Enterprise teams.
---

# vercel_audit_log_drain (Resource)

Provides an Audit Log Drain resource.

Audit Log Drains forward team activity events to an HTTP endpoint or Amazon S3. They apply to the whole team and do not support project selection, filtering, or sampling.

~> Audit Log Drains are only available to Enterprise teams.

## Example Usage

```terraform
resource "vercel_audit_log_drain" "example" {
  name = "security-audit-events"

  http {
    endpoint    = "https://siem.example.com/vercel"
    encoding    = "ndjson"
    compression = "gzip"

    headers = {
      Authorization = "Bearer my-secret-token"
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) A human-readable name for the Audit Log Drain.

### Optional

- `http` (Block, Optional) Configuration for delivery to a custom HTTP endpoint. Exactly one of `http` or `s3` must be configured. (see [below for nested schema](#nestedblock--http))
- `s3` (Block, Optional) Configuration for delivery to an Amazon S3 bucket. Exactly one of `http` or `s3` must be configured. (see [below for nested schema](#nestedblock--s3))
- `team_id` (String) The ID of the team the Audit Log Drain should exist under. Required when configuring a team resource if a default team has not been set in the provider.

### Read-Only

- `id` (String) The ID of the Audit Log Drain.

<a id="nestedblock--http"></a>
### Nested Schema for `http`

Required:

- `encoding` (String) The format used to deliver Audit Log events. Can be `json` or `ndjson`.
- `endpoint` (String) The HTTPS endpoint that receives Audit Log events.

Optional:

- `compression` (String) The compression applied to HTTP request bodies. Can be `gzip` or `none`.
- `headers` (Map of String, Sensitive) Custom headers included in requests to the HTTP endpoint.
- `secret` (String, Sensitive) A custom secret used to sign Audit Log events. If omitted, Vercel generates one.


<a id="nestedblock--s3"></a>
### Nested Schema for `s3`

Required:

- `encoding` (String) The format used to write Audit Log events. Can be `json` or `ndjson`.
- `endpoint` (String) The S3 bucket and optional prefix where Audit Log events are written, using the format `s3://bucket[/prefix]`.
- `region` (String) The AWS region containing the S3 bucket.
- `role_arn` (String) The ARN of the AWS IAM role Vercel assumes to write objects.

Optional:

- `object_acl` (String) The canned ACL sent when writing objects. Can be `private`, `bucket-owner-read`, or `bucket-owner-full-control`.
- `server_side_encryption` (String) The server-side encryption header sent when writing objects. Can be `AES256`, `aws:kms`, or `aws:kms:dsse`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# If importing with a default team configured in the provider, simply use the
# audit_log_drain_id.
terraform import vercel_audit_log_drain.example drn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Alternatively, import using team_id/audit_log_drain_id.
terraform import vercel_audit_log_drain.example team_xxxxxxxxxxxxxxxxxxxxxxxx/drn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```
